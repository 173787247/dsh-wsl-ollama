# dsh-wsl-ollama

WSL 下 DeepSeek Harness 的本地 Ollama 工具：`ollama_status` / `list` / `chat` / `embed`。

可选插件，不在 `install.sh`。

```sh
dsh plugin --profile web add github:173787247/dsh-wsl-ollama
```

默认 `http://127.0.0.1:11434`。

## License

MIT
