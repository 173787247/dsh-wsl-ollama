- insert:
    - id: dsh-wsl-ollama
      name: dsh-wsl-ollama
      config:
        enabled: true
        # empty => auto (WSL uses Windows host from /etc/resolv.conf)
        baseUrl: ""
        defaultModel: ""
        timeoutMs: 120000
        maxPromptChars: 32000
